# Environment Variables Configuration
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Supabase Project URL
# This is your project's API URL from the Supabase dashboard
# Format: https://your-project-id.supabase.co
# Security: PUBLIC - Safe to expose to the client (uses NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPABASE_URL=https://qtgcrpdmxnemndahojsm.supabase.co

# Supabase Anonymous Key
# This is the public anon key from your Supabase project settings
# It has limited permissions and is safe to expose to the client
# Security: PUBLIC - Safe to expose to the client (uses NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF0Z2NycGRteG5lbW5kYWhvanNtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM5NzMyNTAsImV4cCI6MjA3OTU0OTI1MH0.gLAuRE9xN2ETF8dzxsKmMohYHbXx6SWPVPA5Nu0yQ4M

# Supabase Service Role Key (OPTIONAL - for server-side operations only)
# This key has full access to your database and should NEVER be exposed to the client
# Only use this in server-side code (API routes, server components, server actions)
# Security: PRIVATE - NEVER expose to the client (no NEXT_PUBLIC_ prefix)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Node Environment
# Values: development, production, test
# Security: PUBLIC - Safe to expose
# NODE_ENV=development

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. NEVER commit .env.local to version control
# 2. Only use NEXT_PUBLIC_ prefix for values that are safe to expose to the browser
# 3. Keep service role keys and other sensitive credentials without NEXT_PUBLIC_ prefix
# 4. Rotate keys regularly, especially if they may have been compromised
# 5. Use different keys for development, staging, and production environments
# 6. Store production secrets in your hosting platform's environment variables
#    (Vercel, Netlify, etc.) rather than in .env files
# 
# =============================================================================
# REQUIRED VARIABLES
# =============================================================================
# 
# The following variables are REQUIRED for the application to function:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# 
# =============================================================================
